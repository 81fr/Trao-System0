{
    "name": "traof-app",
    "version": "1.0.0",
    "description": "Traof Charity System - Mobile App",
    "scripts": {
        "build": "mkdir -p dist && cp index.html dist/ && cp -r assets dist/",
        "start": "npx cap open android",
        "ios": "npx cap open ios",
        "android": "npx cap open android"
    },
    "dependencies": {
        "@capacitor/android": "^5.0.0",
        "@capacitor/core": "^5.0.0",
        "@capacitor/ios": "^5.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^5.0.0"
    }
}